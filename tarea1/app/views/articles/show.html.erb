
<p>
  <%= @article.created_at %>
</p>

<p>
  <%= @article.titlr %>
</p>

<p>
  <%= @article.bajada %>
</p>

<p>
  <%= @article.body %>
</p>

<% if user_signed_in? %>
  <%= link_to 'Editar', edit_article_path(@article) %> |
<% end %>
<%= link_to 'Volver', articles_path %>
<h1> </h1>

<h1>Comentarios</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Comentario</th>
      <th>nro</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comentaries.each do |comentary| %>
      <tr>
        <td><%= comentary.user_id %></td>
        <td><%= comentary.content %></td>
        <td><%= comentary.article_id %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= form_for(@comentary) do |f| %>

  <% if @comentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentary.errors.count, "error") %> prohibited this comentary from being saved:</h2>

      <ul>
      <% @comentary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= "Contenido" %>
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= "Nombre" %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
